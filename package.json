{
  "name": "redometar-iot",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently --names \"frontend,backend,simulator\" --prefix-colors \"cyan,green,yellow\" \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\" \"npm run simulator\"",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "simulator": "node scripts/run-simulator.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
